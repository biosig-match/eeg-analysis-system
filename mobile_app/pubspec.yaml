name: eeg_visualizer_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # Flutter 3.16以降を推奨

dependencies:
  flutter:
    sdk: flutter
  
  # バージョンを最新に更新
  permission_handler: ^11.3.1
  location: ^5.0.3
  flutter_blue_plus: ^1.32.4
  
  provider: ^6.1.2
  zstandard: ^1.3.27 
  fl_chart: ^0.68.0
  http: ^1.2.1
  intl: ^0.19.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true